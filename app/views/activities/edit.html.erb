<% content_for :title, "Editing activity" %>

<div class="surface-card">
  <div class="surface-card-header">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2">
      <div>
        <h1 class="h3 mb-1">Edit activity</h1>
        <div class="text-muted">Update details, manage images, and keep your attendees informed.</div>
      </div>
      <div class="d-flex gap-2">
        <% activity_nav = @from_profile ? { from: "profile", return_to: params[:return_to] } : {} %>
        <%= link_to "View", activity_path(@activity, **activity_nav), class: "btn btn-outline-secondary btn-sm" %>
        <%= link_to(@from_profile ? "Back to profile" : "Back", @return_to, class: "btn btn-outline-secondary btn-sm") %>
      </div>
    </div>
  </div>

  <div class="surface-card-body">
    <%= render "form", activity: @activity %>
  </div>
</div>
